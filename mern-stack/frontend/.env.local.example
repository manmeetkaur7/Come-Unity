# .env.local.example (frontend)
# Copy this to `.env.local` in the same folder and adjust if needed.
#
# VITE_API_URL tells the React/Vite app where the backend API lives.
# For local development with the Node/Express server on port 5001, use:

VITE_API_URL=http://localhost:5001

